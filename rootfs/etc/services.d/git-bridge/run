#!/usr/bin/with-contenv sh
set -e

# Start the FastAPI app on all interfaces inside the container.
# HA will publish port 8000 as configured in config.yaml.
exec uvicorn app:app --host 0.0.0.0 --port 8000
